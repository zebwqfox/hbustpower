# 1.5.0 引导与界面验证

2026-09-07，Xcode 27 beta，iOS 26.5 iPhone 17 Pro 模拟器。

## 本次验证

- `--verify-first-run`：首次从权限步骤开始；不能提前完成；并发点击只申请一次；返回后不重复申请已授权权限；完成状态持久化。
- 拒绝授权后进入设置分支；从设置返回读取新授权状态；申请失败可重试。
- 暂不开启后，低电量检查不会再次自动弹出权限请求。
- 原生“开始使用”按钮连续点击两次，完成回调只交付一次。
- `--verify-design`：首页核心数据、充值控件按压反馈、图表选中与关联读数、清除选中通过。
- 原有 10 项启动回归、27 项通知回归通过；四个主页面的大标题滚动从 106 点收起至 54 点。
- 已检查首页及首次权限引导截图；Debug 模拟器与 Release 真机目标编译通过。

## 验证边界

权限分支使用可控通知中心验证；没有使用真实账号重新登录，也没有在真实设备上验证通知送达。IPA 未签名，可沿用之前的侧载签名流程。
