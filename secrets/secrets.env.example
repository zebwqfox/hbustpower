# Copy to secrets.env, fill in the real values, then run: ./scripts/secrets.sh lock
# Never commit secrets.env itself — only the encrypted secrets.env.enc.

# Campus card OAuth app key, taken from the school's own portal entry URL.
SCHOOL_APP_KEY=REPLACE_WITH_YOUR_SCHOOL_APP_KEY
